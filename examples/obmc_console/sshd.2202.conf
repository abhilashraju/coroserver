# SSH daemon configuration for port 2202
# This configuration provides SSH access to the IBM i 5250 console client.

# Include common SSH configuration
Include /etc/ssh/sshd_config_common

# Force the 5250-aware client to connect to console_server's IBM i socket.
ForceCommand /usr/bin/ibmi_console_client -s /var/run/obmc-console-ibmi.sock

# Access control - only allow hostconsole group
AllowGroups hostconsole

# Channel timeout - keep sessions alive for up to 60 minutes
ChannelTimeout *=60m

# Disable X11 forwarding
X11Forwarding no

# Disable TCP forwarding
AllowTcpForwarding no

# Disable agent forwarding
AllowAgentForwarding no

# Use privilege separation
UsePrivilegeSeparation sandbox

# Logging
SyslogFacility AUTH
LogLevel INFO

